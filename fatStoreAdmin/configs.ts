export const idLength = 64;
export const nameLength = 128;

export enum Roles {
  designer = "designer",
  seller = "seller",
  producer = "producer",
  visitor = "visitor",
  root = "root",
}

export enum CreatableRoles {
  designer = "designer",
  seller = "seller",
  producer = "producer",
}
